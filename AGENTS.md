# AGENTS.md

## 1. このファイルの目的

このファイルは、このリポジトリを編集するAIエージェント向けの開発指針である。

プロジェクトの詳細な仕様は `local_weather_simulation_spec.md`、人間向けの概要は `README.md`、現在の設計判断・進捗・未解決事項は `MEMORY_PROJECT.md` を参照すること。

優先順位は次の通りとする。

1. ユーザーからその場で与えられた明示的な指示
2. `local_weather_simulation_spec.md`
3. `AGENTS.md`
4. `MEMORY_PROJECT.md`
5. `README.md`

仕様と実装が食い違う場合、勝手に仕様を書き換えず、差異を明示すること。

---

## 2. プロジェクト概要

本プロジェクトは、WRF（Weather Research and Forecasting Model）を利用し、指定地点周辺の局地気象を高解像度で再現・予測するシステムである。

Version 1の主要目標は次の通り。

- 任意地点を中心に計算領域を設定する。
- 過去を含む任意日時を指定できるようにする。
- 9 km → 3 km → 1 km の3段ネストを利用する。
- d03は約100 km四方とし、中心半径約20 kmを主解析対象とする。
- 過去事例をWRFで再現する。
- アメダス観測値と比較する。
- 本校観測装置のデータと比較する。
- MAE・Bias・RMSEなどで定量評価する。
- 気温・湿度・風・降水などを可視化する。
- 時間変化をアニメーションとして表示・出力する。

---

## 3. 最重要の開発方針

### 3.1 完成を優先する

最初からすべてを高機能にしない。

まず、

> 過去の1事例について、1 kmまでのWRFシミュレーションを正常に実行し、観測値との比較とアニメーション表示まで行う

ことを最優先とする。

高解像度化、データ同化、GUI、リアルタイム予測などは、この最小構成が完成した後に追加する。

### 3.2 段階的に実装する

原則として以下の順で進める。

1. WRFを実行できる環境を作る。
2. 9 km単独で過去事例を再現する。
3. 3 kmネストを追加する。
4. 1 kmネストを追加する。
5. PythonでWRF出力を読み込む。
6. 気温・風などを可視化する。
7. アニメーションを生成する。
8. アメダスとの比較を実装する。
9. 本校観測装置との比較を実装する。
10. 複数事例で評価する。
11. 必要なら観測データ同化を追加する。
12. 必要なら約333 mまで高解像度化する。

### 3.3 科学的妥当性を優先する

「動くコード」であるだけでなく、気象学・数値計算として妥当であることを重視する。

特に以下を守る。

- アメダスだけをWRFの3次元初期値・境界値として扱わない。
- 大気の初期値・境界値にはMSM等の適切な3次元気象データを使用する。
- アメダスと本校観測値は、まず検証用データとして扱う。
- 観測値をモデルへ取り込む場合は、単純な値の置換ではなく、OBSGRID、observational nudging、WRFDA等の適切な方法を検討する。
- 高解像度化だけで精度が向上すると仮定しない。
- モデル格子値と点観測値の違い、標高差、観測環境差を考慮する。
- spin-up期間を解析対象から分離する。

---

## 4. 想定環境

主開発環境：

- macOS
- Apple Silicon Mac
- MacBook Air M3
- メモリ 16 GB
- Python 3.12系を基本候補とする
- Python環境・依存関係管理には `uv` を優先する

WRF/WPSは計算負荷が高いため、MacBook Airで現実的に実行できる規模を意識する。

計算量やメモリ消費が大幅に増える変更を行う場合は、その影響を説明すること。

---

## 5. 技術方針

### 5.1 Python

Python側では主に以下を担当する。

- 設定読み込み
- 気象データ取得・整理
- アメダスデータ処理
- 本校観測データ処理
- WRF実行前後の処理
- wrfout解析
- 観測地点への補間
- 統計評価
- 可視化
- アニメーション生成
- 将来的なCLI

候補ライブラリ：

- numpy
- xarray
- pandas
- matplotlib
- netCDF4
- scipy
- PyYAML
- 必要に応じて wrf-python
- MP4生成用に ffmpeg

依存ライブラリを追加するときは、本当に必要か確認すること。

### 5.2 WRF / WPS

WRF本体のソースを本プロジェクトのPythonコードへ混在させない。

WRF/WPSのインストール先、ビルド方法、入力データ、生成物を明確に分離する。

巨大な気象データやWRF出力をGitへコミットしない。

---

## 6. 推奨ディレクトリ構造

```text
weather-simulation/
├── AGENTS.md
├── README.md
├── MEMORY_PROJECT.md
├── local_weather_simulation_spec.md
├── pyproject.toml
├── config/
│   └── default.yaml
├── data/
│   ├── meteorological/
│   │   └── msm/
│   ├── geographic/
│   └── observations/
│       ├── amedas/
│       └── school/
├── src/
│   └── weather_sim/
│       ├── config/
│       ├── data/
│       ├── observations/
│       │   ├── amedas/
│       │   └── school/
│       ├── preprocess/
│       ├── simulation/
│       ├── analysis/
│       └── visualization/
├── scripts/
├── output/
└── tests/
```

実装上の理由があれば変更してよいが、責務の分離を維持すること。

---

## 7. 設定管理

実験条件をPythonコードへ直接埋め込まない。

原則としてYAML等の設定ファイルに分離する。

設定対象には最低限以下を含める。

- 中心緯度・経度
- 対象開始日時
- 対象終了日時
- タイムゾーン
- spin-up時間
- Domain設定
- 主解析半径
- 出力間隔
- 使用する観測データ
- 可視化設定

日時処理ではタイムゾーンを曖昧にしない。内部ではUTCを利用し、入出力時に `Asia/Tokyo` 等へ変換する設計を優先する。

---

## 8. コード品質

### 8.1 基本原則

- 1クラス・1関数の責務を明確にする。
- データ取得、前処理、計算、解析、描画を分離する。
- 気象データ固有の処理をUIやCLIへ混ぜない。
- ファイルパスを各所へハードコードしない。
- マジックナンバーを避け、設定または名前付き定数にする。
- 単位を変数名・型・docstring等から判断できるようにする。
- 緯度・経度、m/s、K、℃、Pa、hPaなどの単位変換を明示する。

### 8.2 型

可能な範囲で型ヒントを使用する。

特に、

- 日時
- 緯度・経度
- 観測値
- 設定オブジェクト
- ファイルパス

の型を曖昧にしない。

### 8.3 エラー処理

以下を区別する。

- データが存在しない
- ダウンロードに失敗した
- 観測値が欠測している
- WRFが異常終了した
- 出力ファイルが壊れている
- 設定値が不正である

エラーを握りつぶさない。

---

## 9. テスト方針

WRF本体を毎回実行するテストだけに依存しない。

Python側では小さな単体テストを作成する。

優先してテストする対象：

- 設定ファイル読み込み
- JST/UTC変換
- 観測CSVの読み込み
- 欠測値処理
- 単位変換
- WRF格子から観測地点への補間
- Bias
- MAE
- RMSE
- 出力ファイル名生成

WRFを使う統合テストは、必要に応じて小規模なテストケースとして分離する。

---

## 10. 観測値の扱い

観測データは元データを変更せず保存し、解析用に正規化したデータを別に生成することを推奨する。

最低限、各観測値に以下の情報を関連付ける。

- timestamp
- timezone
- station_id
- latitude
- longitude
- elevation
- variable
- value
- unit
- quality / missing flag

本校観測装置については、センサー・設置環境・観測高度等が結果に影響するため、メタデータを保存する。

---

## 11. 評価指標

最低限以下を実装する。

### Bias

\[
Bias = \frac{1}{N}\sum_{i=1}^{N}(P_i-O_i)
\]

### MAE

\[
MAE = \frac{1}{N}\sum_{i=1}^{N}|P_i-O_i|
\]

### RMSE

\[
RMSE = \sqrt{\frac{1}{N}\sum_{i=1}^{N}(P_i-O_i)^2}
\]

欠測値を含む場合は、比較可能な時刻のみを利用し、有効サンプル数 `N` も結果に残す。

---

## 12. 可視化方針

Version 1ではGUIより、再現性の高いファイル出力を優先する。

最低限：

- 気温分布図
- 風ベクトル
- 観測値とWRFの時系列比較
- 気温等の時間変化アニメーション
- MP4またはGIF出力

図には可能な限り、

- 対象日時
- 単位
- Domain
- カラーバー
- 観測地点

を表示する。

異なる時刻を比較するアニメーションでは、必要に応じてカラースケールを固定し、色の変化が値の変化を正しく表すようにする。

---

## 13. データ・Git管理

以下は原則としてGit管理しない。

- MSM等の大容量入力データ
- WRF/WPSの大容量生成物
- `wrfout*`
- MP4/GIF等の大容量出力
- 一時ファイル
- キャッシュ

必要に応じて `.gitignore` を整備する。

小さなサンプルデータのみ、テスト目的で `tests/data/` 等に保存してよい。

---

## 14. ドキュメント更新

重要な設計変更を行った場合は、コードだけでなくドキュメントも更新する。

特に次の場合は `MEMORY_PROJECT.md` を更新する。

- 大きな機能を完成した
- 使用するデータソースを決定・変更した
- Domain設定を変更した
- 重要な実験結果が得られた
- WRFのビルド・実行上の問題を解決した
- 設計上の重要な判断を行った
- 新しい既知の問題が見つかった

仕様そのものを変更する場合は `local_weather_simulation_spec.md` も更新する。

---

## 15. AIエージェントが変更を提案するとき

大きな変更では、可能な限り以下を明確にする。

1. 何を変更するか
2. なぜ必要か
3. 科学的・技術的な根拠
4. 計算時間・メモリへの影響
5. Version 1の完成を遅らせないか
6. テスト方法

不明な仕様を勝手に確定しない。実装に重大な影響がある場合は確認する。

一方、細かな命名や内部実装など、後から容易に変更できる事項については合理的な既定値を選び、開発を止めない。
