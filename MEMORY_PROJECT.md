# MEMORY_PROJECT.md

### 2026-09-08 昼都市・夜間ケースを評価（最新、再実行なし）

- 現在の `output/case_20260904T1200_20260904T2000` は都市実験の完走結果、solar対照は `output/old_5` に移動済み。以下の過去の起動パス・「実行中」は当時の記録。今回WRF/WPSは起動・中断せず、既存出力だけ解析した。
- 同じ昼観測・学校センサー98.5 mで再比較。気温RMSE学校1.412→0.956℃（−32.3%）、府中1.467→0.734℃（−50.0%）。RH9.244→5.332、18.462→9.111ポイント。露点も改善。都市面でLH増加・HFX低下、日射平均はほぼ不変。単一事例での改善であり、都市候補を既定にはしない。
- 昼は開始12時の気温学校＋2.82℃・府中＋2.22℃、府中RH Bias−8.07ポイントが残る。風速RMSE学校0.826→0.817、府中0.495→0.508 m/s、高度換算気圧0.414→0.432 hPa。雨量学校1.09／観測1.99 mm、府中1.32／観測2.00 mmで不足。
- **夜 `output/case_20260903T1800_20260904T0200` は `msm_guided.yaml` で都市0・短波補間0。都市候補を試すには `--template config/msm_guided_urban.yaml` を明示する。夜の結果を都市変更の効果として扱わない。** 夜の同時刻solar対照も未実行。
- 夜の気温RMSE学校1.244／府中1.644℃、RH6.703／7.447ポイント、風速1.252／1.767 m/s、学校高度換算気圧1.426 hPa。最大問題は降水の空振り：比較可能な学校47区間34.33／観測0 mm、府中49区間27.96／観測0 mm。学校ピーク9/4 00:50に7.68 mm/10分。学校積算リセット区間は除外。夜の学校露点Bias＋1.79℃。MSM入力由来かWRF内増幅かは未特定、都市モデルはOFF。
- 夜の入力はMSM9/3 03UTC FH0〜15、GFS9/3 00UTC FH3〜18で途中サイクル切替・未来初期値なし。404とは別の精度問題。
- WRF実時間solar3548.32→都市3779.41秒（＋6.5%、＋3.85分）。夜3385.76秒。各1回・934親ステップ、WPS/動画は含まない。夜の速さを変更効果とみなさない。
- 両ケースSUCCESS、CFL/FATAL/NaNログ0。全保存格子監査d01/d02/d03=15/15/85時刻。d01のみ微小負値（昼QCLOUD−4.87e−8、夜−6.45e−7 kg/kg等）、d02/d03指摘なし。出力を変更しない。短い振動は未判定。
- 詳細 `docs/evaluation_urban_day_night.md`。両ケース `analysis/review_urban_night/` に指標・観測ペア・図・安定性。昼は `day/` と `solar/` に分離、比較表・熱収支・イベント・性能・比較図はその親に保存。次は夜の過剰降水の入力/WRF切り分け、同夜都市実験、昼の開始時高温・乾燥を優先する。

### 2026-09-08 公開済み予報サイクル選択を修正（再実行なし）

- 15:52 JSTに16時終了の後側境界18時へ未来の18時初期値を要求していた404を修正。MSM/GFSとも現在UTC以前の初期値だけを候補にし、公開済みの過去予報から必要な有効時刻を選ぶ。
- MSM気圧面/地表面、GFS予報時間別GRIB/索引をHEADまたは非空キャッシュで確認してから気象入力本体・地理データを準備。404は古い候補へ、接続/503等はエラーとして区別。未来開始も対応範囲なら許容し、MSM FH00〜15で丸めた終了境界まで届かなければ期間短縮/公開待ちを案内。外挿・埋め合わせなし。
- `forecast_sources.json` に選択時刻、初期値、有効時刻、予報時間を保存。従来サイクルを使える間は維持。HEAD後の配信状態変化もあり得るため本取得/抽出検査を維持。
- 詳細 `docs/forecast_availability.md`。実HEADでMSM03UTCの2ファイルとGFS00UTC f009本体/索引の200を確認。**ユーザー指定どおりWRF/WPS/入力本体取得は再実行せず、単体テストとHEADのみ。**
- 次の候補は学校CSVがある09-03 18時〜09-04 02時（夜）、09-04 04〜12時（朝）、09-02 09〜17時（別日昼）。9月8日学校CSVは手元にない。都市設定を明示し、同時刻solar対照と比較する。
- Python74テスト成功、既存互換性警告1件。今回の取得修正で予報場の数値や物理設定は変更していないが、未公開時に別サイクルを選ぶため、選択元の記録を比較に使う。

### 2026-09-08 solar完走評価・都市面を変更（現在）

- ケース移動後、最新 `output/case_20260904T1200_20260904T2000` はsolar、`old_3` は標準gq1e−5、`old_4` は水蒸気強化。最新観測・高度メタデータで同条件比較。気温RMSE学校1.41636→1.41216、府中1.47138→1.46683℃と約0.3%の改善だけ。RH9.2746→9.2440、18.4892→18.4623。気圧高度換算0.41432→0.41392 hPa。雨量不足は継続。WRF3544.29→3548.32秒、速度ほぼ不変。
- 全保存領域監査はSUCCESS/CFL/FATAL/NaNログ0。d01のみ微小負QCLOUD−4.81e−8、QRAIN−3.69e−8 kg/kg。d02/d03は範囲指摘なし。元値は変更しない。
- 学校・府中とも土地利用の都市率1、VEGFRA0。LH約1.36/1.49に対してHFX124.75/117.04 W/m²、SMOIS約0.371/0.360で土壌欠測ではない。今回は都市と自然面を分けるNoah＋SLUCMへ変更する候補を実装。原因未確定、候補を既定にしない。
- `config/msm_guided_urban.yaml` はsolarからurban_physics1＋urban_fraction_source gaia2020を追加。UCAR公式のGAIA2020不透水面率を30 MB取得、約1.87 GB展開。既存WPS_GEOGを変更せず別overlay、出典/SHA-256/ファイル一覧をprovenance.jsonへ保存。建物形状・材料は標準値、AH/灌漑は無効。JAXA10m地図も検討したが登録・分類変換が必要で未導入。MSM/GFSは維持。
- 都市率の全metgrid検査を追加（欠測、flag、0〜1、解析中心の都市率0を拒否）。その他都市率0セルはWRF標準値へのfallback件数を保存。
- MPI30分比較は4/6/8の全保存値完全一致、実時間176.08/190.65/181.48秒。d01包含123.11/120.21/134.27秒、各34ステップ。短時間・各1回、表生成時間も含むため全期間高速化とはみなさない。MPI4維持。ベンチマークの表コピーは正式ファイル名に修正済み、初回は2表生成があった。自然終了させ、中断なし。
- 雲6出力の後処理を改善（不要RH導出・帯域外ゼロ寄与層を省略）。全49時刻の値/dtype/NaN完全一致、5回中央値3.011→2.782秒、7.59%短縮。WRF/動画全体の短縮ではない。Python62テスト成功、既存互換性警告1件。
- 詳細 `docs/evaluation_20260908.md`。解析は最新ケース `analysis/review_20260908/`、MPI試行は `output/benchmark_mpi_20260908_v2/`。
- 都市実験 `output/case_20260904T1200_20260904T2000_urban_check_20260908` を12:21 JST起動。18入力検査/real成功、12:24時点で3領域約20分以上進行、CFL/FATAL/NaN0。実wrfinput都市率学校0.822733/府中0.606067。率0の都市fallback候補d01/d02/d03=0/11/55格子、両観測点は該当なし。親70798、WRF71152〜71155（当時値、再確認必須）。**計算を止めず実行中のまま作業終了、完走と精度は未評価。** 観測メタデータは同一、動画なし。起動確認記録はケース内startup_check.json。

### 2026-09-07 水蒸気実験完走の再評価・太陽角補間候補（現在）

- ユーザーがケースを移動。最新 `output/case_20260904T1200_20260904T2000` は前ターン起動したgq=5e−5の完走結果、直前gq=1e−5は `output/old_3`。下の起動パス/PIDは当時の記録で現在とは異なる。
- 同じ最新観測・学校センサー98.5 mの現地気圧メタデータで再評価。気温RMSE学校1.416→1.639℃、府中1.471→1.623℃と悪化。RHは9.275→8.391、18.489→16.941ポイントへ改善。府中風速0.501→0.573 m/sと悪化。気圧高度換算RMSE0.414→0.406 hPa。学校露点0.887→0.586℃、府中2.582→2.078℃。gq=5e−5を既定に昇格しない。
- WRF実時間3544.29→3651.51秒（約3%長い、各1回）。934ステップ同一。SUCCESS、CFL/FATAL/NaN0。全保存領域の監査でd01の微小負水成物のみ指摘（QCLOUD−5.72e−8等）。出力は修正しない。
- 評価を先に完了後、追加された直下の `wether_sim_current_evaluation.md` と `wrf_performance_optimization_plan.md` を読了。両原文は変更しない。報告は `docs/evaluation_moisture_followup.md`、集計は最新ケース `analysis/review_moisture_followup/`。
- 資料にない精度候補としてUCAR推奨の太陽高度短波補間swint_opt=1を追加。`config/msm_guided_solar.yaml` は既定gq=1e−5からこの1項目だけ変更。radt15分、格子、dt、45層、spin-up、MPIと出力間隔は維持。雲の時間変化を放射呼出し間で更新するものではない。精度改善は未保証、対照は `old_3`。
- 資料にない速度改善としてmetgrid実入力・バイナリ・設定のSHA-256キャッシュを追加。全出力ハッシュ照合と全物理検査、独立コピー、既存出力保護。`--no-metgrid-cache` で無効化可能。共有ライブラリ更新時は無効化して比較する。最新実データ18ファイル68.64 MB完全一致、入力ハッシュ0.768秒＋検査復元0.059秒、旧metgrid41.59秒。約40秒の前処理短縮見込みでWRF積分の高速化ではない。Python55テスト成功。
- 太陽角補間実験 `output/case_20260904T1200_20260904T2000_solar_check_20260907` を23:07 JST起動。23:09時点で3領域とも約28分以上進行、CFL/FATAL/NaN0、real成功。キャッシュ実ヒット0.758秒、wrfinput3・wrfbdy1・wrffdda3の計7ファイルが対照old_3とSHA-256完全一致。親PID60031、WRF60084〜60087（当時値、将来再確認）、起動記録はケース内 `startup_check.json`。**ユーザー指定どおり計算を止めず実行中のまま作業終了。完走・精度改善は未確認。** 観測/高度メタデータは同一、動画なし。

### 2026-09-07 正確性修正・水蒸気実験を起動（現在）

- 直近通常ケースは `output/case_20260904T1200_20260904T2000`、前回FDDAは `output/old_2`、FDDAなしは `output/old`。今回もgq=1e−5で、観測指標10項目は前回FDDAと同じ。最新WRF実時間3544.29秒、d01包含3543.07秒（前回3433.41秒より3.2%長い）。
- **学校高度がユーザーから確認できた。** 地理院の地面標高83.5 m＋5階の地上約15 m＝センサー標高約98.5 m、気圧は現地気圧。`config/station_metadata.yaml` に申告元・概算の旨を保存。元CSVは変更しない。
- モデル地表81.077 mからセンサー高度への短層静力学換算（モデルT2/Q2の仮温度、float64、|dz|≤100 m）を別評価行 `pressure_sensor_height` に追加。未換算も保持。最新のBias +0.01085、MAE0.317、RMSE0.41432 hPa。FDDAなしの同高度RMSE1.15053。**以前の「気圧悪化」は高度未換算だったため修正。高度をそろえると気圧も改善。** 設置高±5 mの仮想感度はBias約±0.57 hPa。
- `evaluate-case` はケース内 `observations/station_metadata.yaml` を読み、`--station-metadata` で明示も可能。位置不一致を拒否し、海面更正気圧や高さ不明では換算しない。気温/風/RHの高所・建物影響は未補正。
- metgrid検査を内側初時刻のみ→全3領域6時刻へ拡張。部分NaNを拒否、海上土壌欠測は許容、大気は海上も検査。RH100.110%を実データで確認し、微小過飽和を破損と扱わない広い範囲0〜110%を使用。クリップなし。係数NaN/Infも拒否。
- 2D風向・矢印を東西/南北へ回転し、無風は風向欠測。raw U10/V10と風速は維持。現在の気温静止画と風動画を再生成。3Dは従来から回転済み。
- `audit-case` を追加し保存全格子・全時刻を1時刻ずつ検査。最新3領域はNaN/CFL/FATAL0、圧力/層厚正、上向き圧力減少。d01のみ微小負QCLOUD/QRAIN（最小−4.87e−8/−3.78e−8 kg/kg）、RAINC（−1.76e−8 mm）を指摘。根本原因未特定、出力間の高速振動は未判定。CLI終了1は指摘ありの意味でWRF失敗とは限らない。
- 地点矩形の地表読込を再利用し、7回中央値0.212→0.170秒（19.5%短縮）。11出力配列完全一致。計算刻み・精度は変更なし。再現スクリプト `scripts/benchmark_surface_read.py`。Python52テスト成功、既存互換性警告1件。
- **ユーザーは今回、実行を許可し正常確認後は作業終了、実行中のプロセスは中断しないよう指定。過去の再実行禁止を上書きする。** 水蒸気係数だけ5e−5にした `output/case_20260904T1200_20260904T2000_moisture_check_20260907` を別ケースで起動。全入力検査/real成功、3領域21:30:36 UTCまで進行・CFL/FATAL/NaN0を確認し、**計算を止めず実行中のまま作業終了**。起動親PID45722、WRF46060〜46063（当時値なので将来操作時は必ず再確認）。`launch.log` / `startup_check.json` あり。動画なし、同一観測と高度メタデータをコピー済み。完走後に `evaluate-case` で評価。候補を既定へ昇格しない。
- 詳細は `docs/evaluation_20260907.md`。最新ケース `analysis/review_20260907/` に3条件比較・安定性・ベンチマーク・高度感度を保存。通常 `analysis/verification/` も高度比較込みで更新。

### 2026-09-06 FDDA完走を評価（現在の状態）

- 最新 `output/case_20260904T1200_20260904T2000` はFDDA有効で完走。`output/old` と同一の最新観測CSVで再評価した。対象9月4日12〜20時JST、49時刻。以下の9月5日の「未完走・未検証」は当時の記録。
- 気温RMSE学校5.151→1.416℃／府中5.548→1.471℃、風速1.710→0.826／2.207→0.501 m/sと改善。RH RMSE21.854→9.275／32.772→18.489ポイント。
- 学校気圧RMSE0.971→2.033 hPaへ悪化。学校標高は現CSVで未確定。露点Biasは学校−0.721℃／府中−2.512℃で旧結果からほぼ改善せず、RH改善の多くは気温低下。地表熱収支・乾燥偏差は残る。
- 学校積算雨量から正確な10分差分を作る評価を追加。欠測境界・区間中の欠測・リセットを除外。48区間でRMSE0.0482→0.0513 mm、最新合計モデル1.013／観測1.990 mm。瞬間強度比較よりこちらを主判定に使う。
- d01包含積分時間3651.78→3433.41秒（6.0%短縮）、wrfout3.905→1.887 GB（51.7%減）。両方934ステップ。全工程の実時間ではない。Thompson3表の読込をログで確認。
- 改善済み既定 `config/msm_guided.yaml` は維持。別候補 `config/msm_guided_moisture.yaml` は水蒸気係数のみ1e−5→5e−5 s⁻¹。緩和時間27.8→5.56時間、格子・ステップ数・出力量は同じ。未実行・未検証、気圧を含む他変数の悪化に注意し、勝手に既定へ昇格しない。
- 評価に `moisture_summary.csv` と地点別診断を追加。温度と水蒸気のRH誤差成分、露点指標を保存。観測使用の診断であり、予報の精度補正として扱わない。
- 外部工程の `*.timing.json` を追加し、開始/終了UTC・経過秒・終了コードを保存。Pythonテスト41件成功。既存バイナリ互換性警告1件あり、読込は成功。
- 詳細 `docs/evaluation_20260906.md`、成果物は最新ケース `analysis/review_20260906/`。今回WRF/WPS/realは一切起動していない。再シミュレーション禁止のユーザー指示を継続。

### 2026-09-05 地形付き3D・雲の2D動画を追加（最新）

- 現在残っている対象ケースは `output/old`。9月4日12〜20時JST、49時刻の既存d03から生成した。WRF/WPS再実行は一切していない。
- `analysis/atmosphere_3d.html` は外部ライブラリ・通信不要の単独HTML。地形、気温、東西南北へ回転した風、液水基準RH、QCLOUD、QICE、CLDFRA、W、気圧を表示。ドラッグ・ピンチ、時刻・高度・しきい値・不透明度・高さ強調に対応。元の3D座標をCanvasで投影する。
- 3D既定は主解析半径20 kmに入る格子の外接範囲、水平3格子・鉛直2層ごと、時刻は間引かず49コマ。今回3718点/時刻、地形は元の1 km格子、HTML約10 MB。表示用の間引きであり、2D積算は全格子・全鉛直層で行う。
- 2Dに `cloud_total/low/mid/high/water_path/ice_path_animation.mp4` を追加。CLDFRAの最大ランダム重なり、低層AGL300〜2000 m、中層2000〜6000 m、上層6000 m以上、総雲量は霧も含む。積算は乾燥空気密度×混合比×層厚（kg/m²）、雨・雪・霰は除く。
- `animate-case` は2D+3Dが既定、`--dimension 2d/3d/both`、3D表示用のstride・高度指定を追加。既存出力は通常スキップ、変更を反映する時は `--force`。run-caseの動画生成時にも3Dを追加。
- Pythonテスト36件成功。JSの描画・各要素切替・時刻送り・再生・ドラッグ・ピンチ・モバイル幅変更を純粋単体テストで検証。2D6本はffprobeで49フレーム/1080×840/H.264を確認し、総雲量の静止画も確認した。
- ローカルHTMLのブラウザ表示はブラウザ安全ポリシーにより拒否されたため、実ブラウザでの見た目・スマホ実機テストは未実施。回避用の別ブラウザやサーバーは使っていない。操作手順と制約は `docs/animation_3d.md`。

### 2026-09-05 追加チェック（最新・今回は再シミュレーション禁止）

- 旧結果は `output/old` に移動済み。最新通常ケースは20時JSTまで完走、FDDAなし・親領域10分出力。学校/府中気温Biasは+5.13/+5.52℃で変化なし。d01の包含計算時間合計は3884.20→3651.78秒（約6%減）。親子の時間は重複するため合算しない。
- 前回のFDDAバックグラウンド実験はreal.exeで失敗。ディレクトリは残っていない。`gfdda_end_h` を整数フィールドへ14.0と書いていた不具合を確認し、後側入力までの15（整数）へ修正。Fortran単体の読込で旧値エラー／新値成功を確認したが、WRF/realは今回は一切起動していない。
- `run-case` 既定を `config/msm_guided.yaml` に変更。ナッジングと親領域60分出力の適用漏れを防止。気象精度の改善は未実証。
- 観測評価の派生変数計算前に必要格子範囲を選択。今回9801→14格子、読込・変数導出の5回中央値は約0.271→0.182秒。物理値と元格子位置は維持。
- 降水の区間長を切出し後も保持。学校降水強度N=48→49。先行値不明・積算リセットは欠測扱い。指標と平均値の有効サンプルを一致させる。
- Thompson参照表を同一バイナリ＋チェックサム条件でキャッシュ。完走済み最新結果から準備した（再計算なし）。既存ログには1表だけで45.37秒の生成記録あり。キャッシュ使用時のWRF実測は未実施。
- 詳細は `docs/evaluation_20260905_followup.md`。今ターンのユーザー指示は再シミュレーションをしないこと。
- Pythonテスト33件成功、実データで部分読込と従来評価の8項目一致を確認。NumPy/NetCDFバイナリ互換性警告が1件残る（読込・テストは成功）。

### 2026-09-05 再評価・計算量削減（最新）

- 今回の完走出力は通常ケースで、FDDAなし。前回の指標と完全一致し、FDDA実験はまだ評価できない。詳細は `docs/evaluation_20260905.md`。
- 気温Biasは学校+5.13℃／府中+5.52℃。Q2を比湿として扱っていた評価式を混合比用に修正後、湿度Biasは−21.66／−32.47ポイント。
- `config/case_20260904_fdda_efficient.yaml` にFDDA＋親領域60分出力を設定。d03は10分、格子・45層・spin-upは維持。WRF積分は要求終了まで、WPS/realの後側境界データは維持する。今回の期間ではステップ数約6.7%、履歴フレーム約58%削減。FDDA負荷を含む実時間・精度改善は未検証。
- run-caseは既存wrf_runの上書きを拒否。case.jsonに設定全体・テンプレート・積分終了を保存し、FDDA入力欠落はWRF前に検出する。テスト30件成功。
- 前回のユーザー希望を維持し、再計算開始後の継続監視・停止はしない。

> このファイルは、プロジェクトを別セッション・別AIエージェントへ引き継ぐためのプロジェクトメモである。
>
> 詳細仕様は `docs/local_weather_simulation_spec.md`、AI向け開発規則は `AGENTS.md` を参照すること。
>
> 実装や実験が進んだら、このファイルを継続的に更新する。

---

## 1. プロジェクト概要

### プロジェクト

局地気象シミュレーションシステム

### 目的

WRFを使って指定地点周辺の局地気象を高解像度で再現・予測する。

主解析対象は、

- 指定地点を中心とする半径約20 km

とする。

ただし半径20 kmだけを計算するのではなく、外部から流入する大気を表現するため、より大きな親領域を含むネスティングを行う。

### Version 1の最終目標

> 過去の1事例について、9 km → 3 km → 1 kmのWRFシミュレーションを正常に完走させ、アメダスおよび本校観測装置と比較し、数値評価・グラフ・アニメーションを生成する。

ケースフォルダを入口とする `animate-case`、`prepare-observations`、`evaluate-case`、`cleanup-case` を追加した。削除はdry-runが既定で、再シミュレーション不要時と再評価・動画再生成不要時を別々に指定する。操作手順は `docs/operation_manual.md` に集約する。

---

## 2. 開発環境

現在想定している主環境：

- MacBook Air M3
- RAM 16 GB
- SSD 512 GB
- macOS
- Python
- `uv` によるPython環境管理を優先

WRFはCPU負荷が高く、MacBook Airはファンレスであるため、長時間計算時の熱・計算時間を考慮する。

---

## 3. 現在確定しているDomain構成

### Version 1

| Domain | dx | 範囲目安 | 用途 |
|---|---:|---:|---|
| d01 | 9 km | 約900 × 900 km | 大規模場 |
| d02 | 3 km | 約300 × 300 km | 地方規模 |
| d03 | 1 km | 約100 × 100 km | 局地気象 |

- parent/grid ratio: 原則3:1
- 主解析範囲: d03中心から半径約20 km

d03を解析対象の40 km四方だけにしない理由は、ネスト境界の影響を主解析領域から離すため。

### 将来候補

- d04: 約333 m
- 範囲: 約40～60 km四方

ただしVersion 1完成前には追加しない。

---

## 4. 日時

以下をユーザー指定可能にする。

- 対象開始日時
- 対象終了日時
- 中心地点
- タイムゾーン
- spin-up時間

基本表示タイムゾーン：

```text
Asia/Tokyo
```

内部処理では必要に応じてUTCを使用する。

過去日時を指定できるようにし、まずhindcastを使ってモデルを評価する。

---

## 5. spin-up

Version 1の初期値：

- spin-up: 12時間
- 解析時間: 12時間程度

これは固定仕様ではなく設定ファイルから変更可能にする。

---

## 6. 入力データに関する重要な判断

### アメダスだけではWRFを初期化しない

重要。

アメダスは地上の点観測であり、WRFに必要な上空を含む3次元大気場を持っていない。

したがって、

```text
MSM等の3次元気象データ
        +
アメダス / 本校観測
```

という役割分担にする。

### 初期値・境界値

候補：

- 気象庁 MSM
- その他WRFで利用可能な解析・予報データ

**具体的なデータソースとWRF/WPSへの変換方法はまだ未確定。**

これは次に調査・決定する重要事項。

### アメダス

Version 1では主に検証に使用する。

将来的には、

- OBSGRID
- observational nudging
- WRFDA

等による同化を検討する。

### 本校観測装置

本校の観測値も独立した検証データとして利用する。

最低限、気温を扱う。

取得可能なら、

- 湿度
- 気圧
- 風向
- 風速
- 降水量

等も利用する。

本校観測データの**実際のファイル形式・列構成・観測間隔はまだ仕様化していない**。実データを確認してからReaderを設計する。

---

## 7. 研究上の重要な比較

### 観測された学校－アメダス差

\[
\Delta T_{obs}
=
T_{school}
-
T_{AMeDAS}
\]

### WRFで再現された学校－アメダス差

\[
\Delta T_{WRF}
=
T_{WRF,school}
-
T_{WRF,AMeDAS}
\]

この2つを比較し、

- 学校とアメダスの気温差をWRFが再現できるか
- どの気象条件で差が大きくなるか
- 地形・土地利用・風・地表面状態が関係するか

を調べる。

これは本プロジェクトの重要な研究用途の一つ。

---

## 8. 評価指標

最低限：

- Bias
- MAE
- RMSE
- 有効サンプル数 N

を計算する。

観測欠測時は比較対象から除外し、除外後のNを必ず残す。

将来的には必要に応じて、

- 相関係数
- 最大誤差
- 時刻別誤差
- 日中/夜間別誤差
- 気象条件別誤差

等を追加してよい。

---

## 9. 可視化

Version 1で必要：

- 気温分布
- 風ベクトル
- 観測値 vs WRF 時系列
- シミュレーションの時間変化アニメーション
- MP4またはGIF出力

追加候補：

- 湿度
- 降水
- 気圧
- 地表面温度
- 顕熱フラックス
- 潜熱フラックス
- 境界層高度
- 雲量

アニメーションで時刻間比較を行う場合、カラースケールを毎フレーム自動変更すると変化を誤認しやすいため、比較目的では固定スケールを利用できる設計にする。

---

## 10. 出力間隔

Version 1の標準：

```text
10分
```

設定から変更可能にする。

WRF内部の時間ステップと、解析用ファイルの出力間隔は別物として扱う。

---

## 11. Python側の責務

予定：

```text
src/weather_sim/
├── config/
├── data/
├── observations/
│   ├── amedas/
│   └── school/
├── preprocess/
├── simulation/
├── analysis/
└── visualization/
```

### config

- YAML読み込み
- 設定検証
- 日時・Domain設定

### data

- 気象データ取得
- 地理データ管理

### observations

- アメダスReader
- 本校観測Reader
- 共通観測データモデル

### preprocess

- WRF/WPS向け前処理の制御

### simulation

- WRF実行
- 実行状態管理
- 出力管理

### analysis

- wrfout読み込み
- 地点補間
- MAE/Bias/RMSE
- 学校－アメダス差

### visualization

- 地図
- 時系列
- アニメーション
- MP4/GIF

---

## 12. 設定ファイル案

```yaml
center:
  latitude: 35.69
  longitude: 139.41

time:
  target_start: "2026-08-10T12:00:00+09:00"
  target_end: "2026-08-11T00:00:00+09:00"
  spinup_hours: 12

domains:
  d01:
    dx_m: 9000
    size_km: 900
  d02:
    dx_m: 3000
    size_km: 300
  d03:
    dx_m: 1000
    size_km: 100

analysis:
  radius_km: 20
  output_interval_minutes: 10

observations:
  use_amedas: true
  use_school: true

visualization:
  animation: true
  animation_format: "mp4"
```

これはまだ最終的なスキーマではない。

---

## 13. 現在の進捗

### 完了

- [x] プロジェクトの目的を決定
- [x] 半径20 kmを主解析対象とする方針を決定
- [x] 9 km → 3 km → 1 kmの基本Domain構成を決定
- [x] 過去日時を指定できる仕様を追加
- [x] アメダスによる検証方針を決定
- [x] 本校観測装置による検証方針を追加
- [x] アニメーション可視化を仕様へ追加
- [x] Version 1と将来機能を分離
- [x] 詳細仕様書 `docs/local_weather_simulation_spec.md` を作成
- [x] `pyproject.toml` と `uv` ベースのPython環境を作成
- [x] 型付きYAML設定・JST/UTC変換・設定検証を実装
- [x] 3段ネスト用 `namelist.wps` / `namelist.input` 生成を実装
- [x] 共通観測CSV Reader、欠測・品質処理、温度単位変換を実装
- [x] wrfoutの地上変数読み込みと最近傍地点抽出を実装
- [x] Bias・MAE・RMSE・Nを実装
- [x] 学校－アメダス気温差の観測/WRF比較を実装
- [x] 気温・風の静止画、時系列図、MP4/GIF生成を実装
- [x] 設定検証・namelist生成・解析を行うCLIを実装
- [x] Python側テスト16件（CLI統合確認を含む）を作成

### 未着手・外部データ待ち

- [ ] WRF/WPS環境構築
- [ ] 初期値・境界値データソース確定
- [ ] MSM取得方法の確定
- [ ] MSM → WPS/WRF変換方法の確定
- [ ] 最初の過去事例決定
- [ ] 9 kmテスト
- [ ] ネスティング
- [ ] 実際のアメダス配信形式から共通CSVへの変換
- [ ] 実際の本校観測形式から共通CSVへの変換
- [ ] 実wrfoutと実観測データによる統合確認

---

## 14. 次にやること

優先順位：

### 1. WRF/WPSをMacで動かす方法を確定する

確認事項：

- Apple Siliconでのビルド方法
- gfortran
- MPI
- NetCDF
- HDF5
- WRF
- WPS
- 必要な環境変数

### 2. 初期値・境界値データを確定する

特に、

- MSMを直接利用できるか
- どのデータを取得するか
- 過去データをどこから取得するか
- WPSの`ungrib`へどう入力するか
- Vtableをどうするか

を調査する。

### 3. 最初の過去事例を1つ決める

最初は特殊な豪雨・雷などではなく、比較的安定した事例を選び、まずWRFが正常に動くことを確認する。

### 4. d01 9 kmのみで実行する

いきなり3段ネストにしない。

9 km単独が成功した後、

```text
9 km
↓
9 + 3 km
↓
9 + 3 + 1 km
```

と進める。

### 5. 現在のPython基盤で結果を検証する

実際の `wrfout_d03_*` と共通形式へ変換した観測CSVを用意し、次を実行する。

```bash
uv run weather-sim analyze config/default.yaml \
  --wrfout /path/to/wrfout_d03_* \
  --observations /path/to/observations.csv \
  --station-id school \
  --reference-station-id amedas_station \
  --output-dir output/first_case/analysis \
  --animation
```

---

## 15. Version 1では後回しにするもの

以下は重要だが、最初の完成を遅らせないため後回しにする。

- 約333 m d04
- WRFDA
- OBSGRID
- observational nudging
- リアルタイム自動予測
- GUI
- Web UI
- 高度なインタラクティブ地図
- 多数の物理スキーム比較
- 大規模なパラメータ探索

---

## 16. 未解決事項

今後決める必要がある。

1. プロジェクトの正式名称
2. GitHubリポジトリ名
3. WRF/WPSの具体的なMacビルド方法
4. 初期値・境界値の正式なデータソース
5. MSMの入手・変換方法
6. 使用するWRFバージョン
7. 使用する物理スキーム
8. 鉛直層数
9. WRF内部のtime step
10. d01/d02/d03の正確な格子数
11. 投影法
12. 本校観測データの形式
13. アメダス自動取得方法
14. 最初に再現する過去事例
15. Python 3.12を正式採用するか
16. `wrf-python`を利用するか、xarray中心で実装するか

未解決事項を推測で確定扱いにしない。

---

## 17. 重要な設計判断ログ

### 2026-09-03: 局地シミュレーションを採用

対象地点周辺を細かく計算したいという目的から、広域を一様な高解像度で計算するのではなく、WRFネスティングを利用する方針とした。

### 2026-09-03: 半径20 kmを主解析対象に決定

計算領域そのものは約100 km四方のd03とし、その中央半径20 kmを主に解析する。

### 2026-09-03: 9 km → 3 km → 1 kmをVersion 1に採用

MacBook Air M3 / 16 GBでの実行可能性と局地性のバランスから、まず1 kmまでを目標とする。

### 2026-09-03: アメダスだけで初期化しない

アメダスには3次元大気場がないため、MSM等を初期値・境界値として使用し、アメダスはまず検証へ利用する。

### 2026-09-03: 過去再現を優先

過去日時を指定可能にし、観測値が既知の事例でモデル精度を検証してから将来予測へ進む。

### 2026-09-03: 本校観測装置を評価に使用

アメダスだけでなく本校観測装置を独立した検証点として利用し、学校とアメダスの気温差そのものの再現性も研究対象とする。

### 2026-09-03: アニメーションをVersion 1へ追加

シミュレーション結果を数値・静止画だけでなく、時間変化を把握できるアニメーションとして可視化する。

### 2026-09-03: Python側Version 1基盤を実装

設定検証、3段ネストnamelist生成、共通観測CSV、wrfout読込、最近傍格子抽出、統計評価、学校－アメダス差、静止画・時系列・アニメーション、CLIを実装した。CLI統合確認を含むテスト16件は成功している。

外部入力が未確定のため、次はMSM取得・WPS変換方式と学校観測元形式を決定する必要がある。生成する物理スキームは実行開始用の暫定値であり、事例・WRFバージョン・入力データ確定後に科学的妥当性を再評価する。既定格子数は3:1ネスト制約を満たす100点とし、実幅は891 km、297 km、99 kmとした。

### 2026-09-04: 2026-09-01実データ事例を前処理

- 学校・予測地点を `35.69247912845154, 139.41296806119965` に設定した。
- 初回事例は2026-09-01 15:00–21:00 JST、spin-up 6時間とした。
- 学校WSNログはCP932・1分値・14列で、9月1～3日のファイルを共通観測形式へ変換した。
- 最寄りの気象庁アメダスは府中（block 1133、地点コード44116、約6.5 km、標高59 m）とし、9月1日の10分値を取得・変換した。
- WRF v4.8.0とWPS v4.7.0をApple Silicon arm64向けにビルドした。WPSは同梱Jasper 1.900.29を静的リンクした。
- 公式WPS高解像度必須地理データを取得し、3段ネストの `geogrid` を完了した。
- ネスト中心式の1始まり格子のずれを修正し、学校地点はd01/d02/d03すべて `(y=49, x=49)` に一致した。d03地形標高は約81.1 m。
- 京都大学生存圏研究所のアーカイブからMSM気圧面・地表面GRIB2を取得し、00/03/06/09/12 UTCのWPS中間形式へ変換した。
- MSM単独で3領域・5時刻の `metgrid` を完了した。
- `real.exe` はMSMの17気圧面を認識するが、Noah LSMに必要な土壌温度・土壌水分4層がないため停止することを確認した。定数や気候値で代用せず、同時刻GFS等の土壌場を補助入力として併合する。
- Pythonテストは19件成功。

次工程は、GFSの地表・土壌項目を対象領域だけ取得し、MSMを優先した `metgrid` 併合を行う。その後 `real.exe` を完了し、AGENTS.mdの方針どおりd01単独、d01+d02、3段ネストの順にWRFを実行する。

### 2026-09-04: 任意日時の自動取得・実行ワークフローを追加

- `weather-sim run-case --start ... --end ...` と `scripts/run_weather_case.sh` を追加した。
- 対象期間とspin-upからMSMの3時間境界へ計算期間を自動拡張する。解析期間自体はユーザー指定時刻を維持する。
- 京大RISHのMSM気圧面・地表面データを取得し、有効時刻ごとのGRIB2へ抽出する。
- NOAA GFS公開アーカイブの索引とHTTP Rangeを使い、Noah LSMに必要な地表面・4層土壌レコードのみ取得する。
- WPS高解像度必須地理データは日付非依存キャッシュとして再利用し、存在しない場合だけ公式配布元から取得する。
- MSMを大気場の優先入力、GFSを地表面・土壌補完として `metgrid` へ渡し、`real.exe`、3段ネストWRF、指定期間の静止画・アニメーションまで連続実行する。
- ユーザー指定により、この変更ではテストおよび実計算を実行していない。

### 2026-09-04: 標準アニメーションを6種類へ拡張

- 従来の2 m気温に加え、10 m風速＋風ベクトル、2 m相対湿度、時間降水量、地表気圧、地表面温度を `--animation` で一括生成するようにした。
- 時間変化を比較できるよう、各動画内ではカラースケールを固定する。
- 2026-09-01のd03実出力から6本のMP4を生成し、`ffprobe` で各ファイルの再生時間を確認した。

### 2026-09-04: 実観測多変数評価と地図付き日本語動画

- `evaluate-observations` CLIを追加し、学校・府中アメダスの気温、湿度、風速、気圧、降水をWRF出力時刻単位で評価するようにした。
- 1分観測を同じWRF値へ繰り返し対応させず、各WRF出力時刻に最も近い観測を1件だけ利用する。
- 評価結果としてBias・MAE・RMSE・N、平均値、格子距離、観測点/モデル標高、全対応値、JST時系列図を保存する。
- 6種類の動画へ国土地理院標準タイル、日本語ラベル、JST時刻、学校地点マーカー、出典表記を追加した。
- 2026-09-01実データで9項目を評価し、6本の地図付きMP4を再生成した。既存テスト19件は成功した。

### 2026-09-04: GFS土壌初期値の破損を修正

- 初回WRF結果は学校気温のBias -8.53 ℃、MAE 8.53 ℃、RMSE 9.34 ℃で、夕方以降に地表面温度が約7 ℃まで不自然に低下していた。
- 原因調査により、空間切り出ししたGFS GRIB2は原本の学校付近に約298～304 Kの地中温度を保持する一方、WPS中間形式では欠測ビットマップの扱いが崩れ、学校地点の4層がすべて0 Kになっていたことを確認した。
- GFS全球ファイルの索引から必要な地表・4層土壌12レコードをそのまま取得する方式へ統一した。学校地点の地中温度は5時刻とも約299～303 K、d03陸域は285～309 Kへ正常化した。
- GFS Range取得はレコード単位で最大4回再試行し、接続切断時に正常取得済みレコードを保持する。
- WPSの既定探索パスに合わせ、`GEOGRID.TBL` と `METGRID.TBL` をケース内の `geogrid/`、`metgrid/` に配置するよう修正した。
- `metgrid` 後に最内側領域の陸上地中温度を検査し、180～350 K外ならWRFを開始せずエラーにする。
- 修正版全球GFSを使用した3領域・5時刻の `geogrid`、MSM/GFS `ungrib`、`metgrid` は正常終了した。WRF本計算による再評価は次工程とする。
- MSMの地上気温・相対湿度は1.5 mをスケール付きGRIB2値で表すが、WPS 4.7はそのスケールを参照せず2 m以外を読み飛ばす。そのため抽出時にWPSの2 m地表場としてメタデータを正規化した。
- 正規化後は `met_em` の学校地点に気温297.07 K、相対湿度81.67 %が入り、`real.exe` の `Missing surface temp/RH` 警告は解消した。修正前の初期T2は296.19 Kだった。
- WRF開始前検査を地中温度だけでなく地表気温・地表相対湿度にも拡張した。

### 2026-09-04: macOSでのMSM HTTPS証明書エラーへ対応

- `uv` のOpenSSL版Pythonでは、京都大学RISH配布サーバーに対して `CERTIFICATE_VERIFY_FAILED: unable to get local issuer certificate` が発生する環境があることを確認した。
- 通常のHTTPS取得には `certifi` のCAバンドルを明示し、それでも検証できない場合はmacOSのシステム信頼ストアを使う標準 `curl` へ自動フォールバックするようにした。TLS証明書検証は無効化しない。
- RISHの2026-09-03ディレクトリを実際に取得し、フォールバック経路の動作を確認した。Pythonテスト21件も成功した。

### 2026-09-04: MSM/GFS初期時刻の選択を分離

- 計算開始が21 UTCのケースで、MSMと同じ21 UTCをGFS初期時刻として選び、存在しない `gfs.t21z` が404になる問題を確認した。
- MSMは3時間間隔・最大15時間予報、GFSは6時間間隔・最大120時間予報として独立にサイクルを割り当てるよう修正した。
- 2026-09-03 21 UTC～2026-09-04 12 UTCの入力には、MSM 21 UTCサイクルとGFS 18 UTCサイクルのf003～f018を使用する。

### 2026-09-05: 実観測評価と解析ナッジング感度実験

- 2026-09-04 12:00～20:00 JSTの学校・府中観測で評価し、学校気温Bias +5.13℃、湿度Bias -20.94%、風速Bias +1.29 m/sを確認した。府中でも同符号の誤差だった。
- MSMの学校最近傍値は12時JSTで約24.24℃・84.9%と観測に近い一方、WRFは29.47℃・50.5%へずれた。学校格子は都市カテゴリ・植生率0%で、対象期間の平均顕熱約103 W/m²に対して潜熱約1.8 W/m²だった。
- 入力や時刻変換ではなく、WRF内部で雲の放射効果と湿潤な地表状態を弱く再現して都市面を過熱したことが主因候補である。
- 原因を分離する最初の感度実験として、物理スキームを維持したまま3時間間隔MSMへの3次元解析ナッジングを設定可能にした。実験設定は `config/case_20260904_fdda.yaml`。
- NOAA公式GFSアーカイブでf003、f006、f009、f012、f015、f018の各索引と必要12レコードの存在を確認した。Pythonテスト23件は成功した。
